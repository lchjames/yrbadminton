{
  "name": "yr-badminton-d1",
  "private": true,
  "version": "1.1.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init:remote": "wrangler d1 execute yr-badminton --remote --file=./schema.sql",
    "db:init:local": "wrangler d1 execute yr-badminton --local --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
